<%- include ('partials/header.ejs'); %>

<div class="container mt-5">
    <div class="row">
      <div class="col-md-7">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
              <td>Estudio_Musica</td>
              <td>Cliente</td>
            </tr>
          </thead>
          <tbody>
            <% if (data) { %>
              <% for(var i = 0; i < data.length; i++) { %>
                <tr>
                  <td><%= data[i].nombre_estudio %></td>
                  <td><%= data[i].nombre_cliente %></td>
                  <td>
                    <a href="/estuio_has_clientes/update/<%= data[i].id_Union %>" class="btn btn-info">Edit</a>
                    <a href="/estuio_has_clientes/delete/<%= data[i].id_Union %>" class="btn btn-danger">Delete</a>
                  </td>
                </tr>
              <% } %>
            <% } %>
          </tbody>
        </table>
      </div>

      <div class="col-md-5">
        <div class="card-body">
  
          <form action="/estuio_has_clientes/add" method="post">
            <div class="form-group">
                <input type="number" name="Estudio_Musica_id_estudio" placeholder="id estudio" class="form-control" autofocus>
            <div class="form-group">
              <input type="number" name="Cliente_id_Cliente" placeholder="id cliente" class="form-control" autofocus>
            </div>
  
            <button type="submit" class="btn btn-primary">
              Add estudio
            </button>
          </form>
        </div>
      </div>
      
      <div class="card-body">
        <form action="/" method="GET">
          <button type="submit" class="btn btn-danger">
            Return
          </button>
        </form>
      </div>
    </div>
  </div>

  

<%- include ('partials/footer.ejs'); %>